<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.bookservice.repository.report.ReportInfoMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">

        insert into reportInfo(writer_id, isbn, date, upload_file_name, store_file_name, content, hash_tag, favorite_num, collection_num, disclosure)
        values (#{writerId}, #{isbn}, (date_format(now(), '%Y%m%d%H%i%s')), #{uploadFileName}, #{storeFileName}, #{content}, #{hashTag}, 0, 0,  #{disclosure})
    </insert>

    <!--
    <update id="update">
        update item
        set item_name=#{updateParam.itemName},
        price=#{updateParam.price},
        quantity=#{updateParam.quantity}
        where id = #{id}
    </update>
    -->

    <select id="findById" resultType="project.bookservice.domain.report.ReportInfo">
        select *
        from reportInfo
        where id = #{id}
    </select>

<!--    <select id="findByEmail" resultType="int">-->
<!--        select count(user_email)-->
<!--        from member-->
<!--        where user_email = #{userEmail}-->
<!--    </select>-->


    <select id="findAll" resultType="project.bookservice.domain.report.ReportInfo">
        select *
        from reportInfo
    </select>

<!--    <select id="findAll" resultType="Member">-->
<!--        select id, item_name, price, quantity-->
<!--        from item-->
<!--        <where>-->
<!--            <if test="itemName != null and itemName != ''">-->
<!--                and item_name like concat('%', #{itemName}, '%')-->
<!--            </if>-->
<!--            <if test="maxPrice != null">-->
<!--                and price $lt;= #{maxPrice}-->
<!--            </if>-->
<!--        </where>-->
<!--    </select>-->

</mapper>